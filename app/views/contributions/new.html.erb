<% content_for :nav_active, 'contribuer' %>
<% content_for :title, t('contribution_form.page_title') %>

<% content_for :styles do %>
  <style>
    /* === CONTRIBUTION FORM STYLES === */

    .form-container {
      max-width: 700px;
      margin: 3rem auto;
      padding: 0 2rem;
    }

    .back-link {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      color: var(--color-text-muted);
      text-decoration: none;
      font-size: 0.9rem;
      margin-bottom: 2rem;
      transition: color 0.2s;
    }

    .back-link:hover {
      color: var(--color-primary);
    }

    .form-container h1 {
      font-family: var(--font-display);
      font-size: 2.5rem;
      font-weight: 800;
      color: var(--color-dark);
      margin-bottom: 0.5rem;
    }

    .intro {
      color: var(--color-text-muted);
      font-size: 1rem;
      margin-bottom: 2.5rem;
      line-height: 1.6;
    }

    .form-section {
      background: white;
      border: 1px solid var(--color-border);
      border-radius: var(--radius-md);
      padding: 2rem;
      margin-bottom: 2rem;
    }

    .form-section h2 {
      font-family: var(--font-display);
      font-size: 1.3rem;
      font-weight: 700;
      color: var(--color-dark);
      margin-bottom: 1.5rem;
    }

    .type-selector {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 1rem;
      margin-bottom: 2rem;
    }

    .type-option {
      padding: 1.2rem;
      border: 2px solid var(--color-border);
      border-radius: var(--radius-md);
      cursor: pointer;
      transition: all 0.2s;
      text-align: center;
    }

    .type-option:hover {
      border-color: var(--color-primary);
      background: #FFF8E7;
    }

    .type-option.selected {
      border-color: var(--color-primary);
      background: #FFF8E7;
      border-width: 3px;
    }

    .type-option .emoji {
      font-size: 2rem;
      display: block;
      margin-bottom: 0.5rem;
    }

    .type-option .label {
      font-weight: 600;
      font-size: 0.95rem;
      color: var(--color-dark);
    }

    .form-group {
      margin-bottom: 1.5rem;
    }

    .form-group label {
      display: block;
      font-weight: 600;
      margin-bottom: 0.5rem;
      color: var(--color-dark);
      font-size: 0.95rem;
    }

    .form-group label .required {
      color: var(--color-primary-hover);
    }

    .form-group input[type="text"],
    .form-group input[type="email"],
    .form-group input[type="url"],
    .form-group textarea {
      width: 100%;
      padding: 0.8rem;
      border: 2px solid var(--color-border);
      border-radius: var(--radius-sm);
      font-family: var(--font-body);
      font-size: 0.95rem;
      transition: border-color 0.2s;
    }

    .form-group input:focus,
    .form-group textarea:focus {
      outline: none;
      border-color: var(--color-primary);
    }

    .form-group textarea {
      resize: vertical;
      min-height: 120px;
    }

    .hint {
      font-size: 0.85rem;
      color: var(--color-text-light);
      margin-top: 0.3rem;
    }

    .file-upload-zone {
      border: 2px dashed var(--color-border);
      border-radius: var(--radius-sm);
      padding: 2rem;
      text-align: center;
      background: var(--color-bg);
      transition: all 0.2s;
      cursor: pointer;
      display: block;
    }

    .file-upload-zone:hover,
    .file-upload-zone.dragover {
      border-color: var(--color-primary);
      background: #FFF8E7;
    }

    .upload-icon {
      font-size: 3rem;
      margin-bottom: 1rem;
    }

    .upload-text {
      font-weight: 600;
      color: var(--color-dark);
      margin-bottom: 0.5rem;
    }

    .preview-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(120px, 1fr));
      gap: 1rem;
      margin-top: 1rem;
    }

    .preview-item {
      position: relative;
      aspect-ratio: 1;
      border-radius: var(--radius-sm);
      overflow: hidden;
      border: 2px solid var(--color-border);
    }

    .preview-item img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    .remove-photo {
      position: absolute;
      top: 5px;
      right: 5px;
      background: rgba(0,0,0,0.7);
      color: white;
      border: none;
      border-radius: 50%;
      width: 24px;
      height: 24px;
      cursor: pointer;
      font-size: 16px;
      line-height: 1;
    }

    .submit-btn {
      width: 100%;
      padding: 1rem;
      background: var(--color-primary);
      color: var(--color-dark);
      border: none;
      border-radius: var(--radius-sm);
      font-size: 1.05rem;
      font-weight: 700;
      cursor: pointer;
      transition: all 0.2s;
      font-family: var(--font-display);
    }

    .submit-btn:hover {
      background: var(--color-primary-hover);
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(245, 168, 0, 0.3);
    }

    .submit-btn:disabled {
      background: #ccc;
      cursor: not-allowed;
      transform: none;
    }

    @media (max-width: 640px) {
      .type-selector {
        grid-template-columns: 1fr;
      }
    }
  </style>
<% end %>

<div class="form-container">
  <a href="<%= root_path %>" class="back-link"><%= t('contribution_form.back') %></a>

  <h1><%= t('contribution_form.title') %></h1>
  <p class="intro"><%= t('contribution_form.intro') %></p>

  <%= form_with model: @contribution, local: true, multipart: true do |f| %>
    <div class="form-section">
      <h2><%= t('contribution_form.type_title') %> <span class="required"><%= t('contribution_form.required') %></span></h2>

      <div class="type-selector">
        <div class="type-option" data-type="fle">
          <span class="emoji">📚</span>
          <span class="label"><%= t('contribution_form.type_fle') %></span>
        </div>
        <div class="type-option" data-type="event">
          <span class="emoji">🎉</span>
          <span class="label"><%= t('contribution_form.type_event') %></span>
        </div>
        <div class="type-option" data-type="enterprise">
          <span class="emoji">🚀</span>
          <span class="label"><%= t('contribution_form.type_enterprise') %></span>
        </div>
        <div class="type-option" data-type="portrait">
          <span class="emoji">✨</span>
          <span class="label"><%= t('contribution_form.type_portrait') %></span>
        </div>
      </div>

      <%= f.hidden_field :contribution_type, id: 'contribution_type' %>
    </div>

    <div class="form-section">
      <h2><%= t('contribution_form.info_title') %></h2>

      <div class="form-group">
        <label><%= t('contribution_form.label_title', default: 'Titre') %> <span class="required">*</span></label>
        <%= f.text_field :title, placeholder: t('contribution_form.placeholder_title'), required: true %>
      </div>

      <div class="form-group">
        <label><%= t('contribution_form.label_description', default: 'Description') %> <span class="required">*</span></label>
        <%= f.text_area :description, placeholder: t('contribution_form.placeholder_description'), required: true %>
        <p class="hint"><%= t('contribution_form.hint_description') %></p>
      </div>

      <div class="form-group">
        <label><%= t('contribution_form.label_address', default: 'Adresse complète') %> <span class="required">*</span></label>
        <%= f.text_field :location, placeholder: t('contribution_form.placeholder_address'), required: true %>
        <p class="hint"><%= t('contribution_form.hint_address') %></p>
      </div>

      <div class="form-group">
        <label><%= t('contribution_form.label_contact') %></label>
        <%= f.text_field :contact_info, placeholder: t('contribution_form.placeholder_contact', default: 'Ex: contact@exemple.fr ou 06 12 34 56 78') %>
        <p class="hint"><%= t('contribution_form.hint_contact') %></p>
    </div>
    <div class="form-section">
      <h2><%= t('contribution_form.photos_title', default: 'Photos') %></h2>
      <div class="form-group">
        <label for="file-input" class="file-upload-zone" id="upload-zone">
          <div class="upload-icon">📸</div>
          <div class="upload-text"><%= t('contribution_form.photos_upload', default: 'Cliquez ou glissez vos photos ici') %></div>
          <p class="hint"><%= t('contribution_form.photos_hint', default: 'Formats acceptés : JPG, PNG (max 5 Mo) — jusqu\'à 5 photos') %></p>
        </label>
        <%= f.file_field :photos, multiple: true, accept: 'image/*', id: 'file-input', style: 'display: none;' %>
        <div id="preview-container" class="preview-grid" style="display: none;"></div>
      </div>
    </div>
    <div class="form-section">
    </div>

    <div class="form-section">
      <h2><%= t('contribution_form.signature_title') %></h2>

      <div class="form-group">
        <label><%= t('contribution_form.label_name') %></label>
        <%= f.text_field :author_name, placeholder: t('contribution_form.placeholder_name') %>
        <p class="hint"><%= t('contribution_form.hint_name') %></p>
      </div>

      <div class="form-group">
        <label><%= t('contribution_form.label_flag') %></label>
        <%= f.text_field :author_flag, placeholder: '🇫🇷', maxlength: 2 %>
        <p class="hint">Ex: 🇫🇷 🇺🇦 🇸🇾 🇲🇦 🇨🇩</p>
      </div>
    </div>

    <%= f.submit t('contribution_form.submit'), class: 'submit-btn', id: 'submit-btn', disabled: true %>
  <% end %>
</div>

<% content_for :scripts do %>
  <script>
    // Type selector
    const typeOptions = document.querySelectorAll('.type-option');
    const typeInput = document.getElementById('contribution_type');
    const submitBtn = document.getElementById('submit-btn');

    typeOptions.forEach(option => {
      option.addEventListener('click', () => {
        typeOptions.forEach(opt => opt.classList.remove('selected'));
        option.classList.add('selected');
        typeInput.value = option.dataset.type;
        submitBtn.disabled = false;
      });
    });

    // Photo upload
    const fileInput = document.getElementById('file-input');
    const uploadZone = document.getElementById('upload-zone');
    const previewContainer = document.getElementById('preview-container');

    fileInput.addEventListener('change', handleFiles);

    uploadZone.addEventListener('dragover', (e) => {
      e.preventDefault();
      uploadZone.classList.add('dragover');
    });

    uploadZone.addEventListener('dragleave', () => {
      uploadZone.classList.remove('dragover');
    });

    uploadZone.addEventListener('drop', (e) => {
      e.preventDefault();
      uploadZone.classList.remove('dragover');
      fileInput.files = e.dataTransfer.files;
      handleFiles();
    });

    function handleFiles() {
      const files = Array.from(fileInput.files);
      if (files.length === 0) return;

      previewContainer.style.display = 'grid';
      previewContainer.innerHTML = '';

      files.forEach((file, index) => {
        if (file.type.startsWith('image/')) {
          const reader = new FileReader();
          reader.onload = (e) => {
            const preview = document.createElement('div');
            preview.className = 'preview-item';
            preview.innerHTML = `
              <img src="${e.target.result}" alt="Preview ${index + 1}">
              <button type="button" class="remove-photo" data-index="${index}">×</button>
            `;
            previewContainer.appendChild(preview);
          };
          reader.readAsDataURL(file);
        }
      });
    }

    previewContainer.addEventListener('click', (e) => {
      if (e.target.classList.contains('remove-photo')) {
        const index = parseInt(e.target.dataset.index);
        const dt = new DataTransfer();
        const files = Array.from(fileInput.files);
        files.forEach((file, i) => {
          if (i !== index) dt.items.add(file);
        });
        fileInput.files = dt.files;
        handleFiles();
        if (dt.files.length === 0) {
          previewContainer.style.display = 'none';
        }
      }
    });
  </script>
<% end %>
