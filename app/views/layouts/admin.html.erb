<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin - Les Tisserands</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <link href="https://fonts.googleapis.com/css2?family=Fraunces:ital,wght@0,600;0,800&family=DM+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">
  
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    
    body {
      font-family: 'DM Sans', sans-serif;
      background: #f5f5f5;
      display: flex;
      min-height: 100vh;
    }
    
    .sidebar {
      width: 260px;
      background: #0F0E0A;
      color: white;
      padding: 2rem 0;
      position: fixed;
      height: 100vh;
      overflow-y: auto;
    }
    
    .logo {
      padding: 0 1.5rem;
      margin-bottom: 2rem;
      display: flex;
      align-items: center;
      gap: 0.7rem;
    }
    
    .logo-text {
      font-family: 'Fraunces', serif;
      font-size: 1.5rem;
      font-weight: 800;
    }
    
    .admin-badge {
      background: #F5A800;
      color: #0F0E0A;
      padding: 0.3rem 0.7rem;
      border-radius: 12px;
      font-size: 0.75rem;
      font-weight: 700;
      margin-left: auto;
    }
    
    .nav-menu {
      list-style: none;
    }

    .nav-separator {
      height: 1px;
      background: rgba(255,255,255,0.08);
      margin: 0.8rem 1.5rem;
    }
    
    .nav-item {
      margin: 0.3rem 0;
    }
    
    .nav-link {
      display: flex;
      align-items: center;
      gap: 0.8rem;
      padding: 0.9rem 1.5rem;
      color: #999;
      text-decoration: none;
      transition: all 0.2s;
      font-size: 0.95rem;
    }
    
    .nav-link:hover,
    .nav-link.active {
      background: rgba(245, 168, 0, 0.1);
      color: #F5A800;
      border-left: 3px solid #F5A800;
    }
    
    .nav-icon {
      font-size: 1.2rem;
    }
    
    .logout-link {
      color: #ff6b6b;
      margin-top: 2rem;
    }
    
    .main-content {
      margin-left: 260px;
      flex: 1;
      padding: 2rem;
    }
    
    .header {
      background: white;
      padding: 1.5rem 2rem;
      border-radius: 12px;
      margin-bottom: 2rem;
      box-shadow: 0 2px 8px rgba(0,0,0,0.05);
    }
    
    .header h1 {
      font-family: 'Fraunces', serif;
      font-size: 2rem;
      color: #0F0E0A;
    }
    
    .stats-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 1.5rem;
      margin-bottom: 2rem;
    }
    
    .stat-card {
      background: white;
      padding: 1.5rem;
      border-radius: 12px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.05);
    }
    
    .stat-value {
      font-family: 'Fraunces', serif;
      font-size: 2.5rem;
      font-weight: 800;
      color: #F5A800;
      margin-bottom: 0.3rem;
    }
    
    .stat-label {
      color: #666;
      font-size: 0.9rem;
    }
    
    .btn {
      display: inline-block;
      padding: 0.7rem 1.3rem;
      border-radius: 8px;
      text-decoration: none;
      font-weight: 600;
      font-size: 0.9rem;
      transition: all 0.2s;
      border: none;
      cursor: pointer;
    }
    
    .btn-primary {
      background: #F5A800;
      color: #0F0E0A;
    }
    
    .btn-primary:hover {
      background: #E8920A;
    }
    
    .btn-success {
      background: #4CAF50;
      color: white;
    }
    
    .btn-danger {
      background: #ff6b6b;
      color: white;
    }
    
    .btn-secondary {
      background: #666;
      color: white;
    }
    
    table {
      width: 100%;
      background: white;
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 2px 8px rgba(0,0,0,0.05);
    }
    
    th {
      background: #0F0E0A;
      color: white;
      padding: 1rem;
      text-align: left;
      font-weight: 600;
    }
    
    td {
      padding: 1rem;
      border-bottom: 1px solid #f0f0f0;
    }
    
    tr:hover {
      background: #fafafa;
    }
    
    .badge {
      display: inline-block;
      padding: 0.3rem 0.8rem;
      border-radius: 12px;
      font-size: 0.8rem;
      font-weight: 600;
    }
    
    .badge-published {
      background: #E4F3EC;
      color: #3A7A5A;
    }
    
    .badge-pending {
      background: #FFF8E7;
      color: #E8920A;
    }
    
    .actions {
      display: flex;
      gap: 0.5rem;
    }
  </style>
</head>
<body>

<aside class="sidebar">
  <div class="logo">
    <span style="font-size: 1.8rem;">🧵</span>
    <span class="logo-text">Les Tisserands</span>
    <span class="admin-badge">ADMIN</span>
  </div>
  
  <ul class="nav-menu">
    <li class="nav-item">
      <%= link_to admin_root_path, class: "nav-link #{current_page?(admin_root_path) ? 'active' : ''}" do %>
        <span class="nav-icon">📊</span>
        <span>Dashboard</span>
      <% end %>
    </li>

    <li><div class="nav-separator"></div></li>

    <li class="nav-item">
      <%= link_to admin_contributions_path, class: "nav-link #{controller_name == 'contributions' && action_name == 'index' ? 'active' : ''}" do %>
        <span class="nav-icon">🧵</span>
        <span>Partages</span>
      <% end %>
    </li>
    <li class="nav-item">
      <%= link_to new_admin_contribution_path, class: "nav-link #{controller_name == 'contributions' && action_name == 'new' ? 'active' : ''}" do %>
        <span class="nav-icon">➕</span>
        <span>Nouveau partage</span>
      <% end %>
    </li>

    <li><div class="nav-separator"></div></li>

    <li class="nav-item">
      <%= link_to admin_portraits_path, class: "nav-link #{controller_name == 'portraits' && action_name == 'index' ? 'active' : ''}" do %>
        <span class="nav-icon">✨</span>
        <span>Trajectoires</span>
      <% end %>
    </li>
    <li class="nav-item">
      <%= link_to new_admin_portrait_path, class: "nav-link #{controller_name == 'portraits' && action_name == 'new' ? 'active' : ''}" do %>
        <span class="nav-icon">➕</span>
        <span>Nouvelle trajectoire</span>
      <% end %>
    </li>

    <li><div class="nav-separator"></div></li>

    <li class="nav-item">
      <%= link_to root_path, class: "nav-link" do %>
        <span class="nav-icon">🌐</span>
        <span>Voir le site</span>
      <% end %>
    </li>
    <li class="nav-item">
      <%= button_to destroy_admin_session_path, method: :delete, class: "nav-link logout-link", style: "border: none; background: none; width: 100%; text-align: left; cursor: pointer;" do %>
        <span class="nav-icon">🚪</span>
        <span>Déconnexion</span>
      <% end %>
    </li>
  </ul>
</aside>

<main class="main-content">
  <% if notice %>
    <div style="background: #E4F3EC; border-left: 4px solid #4CAF50; padding: 1rem; border-radius: 8px; margin-bottom: 1.5rem;">
      <%= notice %>
    </div>
  <% end %>
  
  <% if alert %>
    <div style="background: #FFE5E5; border-left: 4px solid #ff6b6b; padding: 1rem; border-radius: 8px; margin-bottom: 1.5rem;">
      <%= alert %>
    </div>
  <% end %>
  
  <%= yield %>
</main>

</body>
</html>
